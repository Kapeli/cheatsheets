cheatsheet do
  title 'Mac OS X Keybindings'
  docset_file_name 'Mac_OSX_Keybindings'
  keyword 'osx'
  source_url 'http://www.hcs.harvard.edu/~jrus/site/system-bindings.html'

  category do
    id 'New line'

    entry do
      command '↩'
      name 'new line'
    end
    entry do
      command '⌥ ↩'
      name "new line (don't leave form box)"
    end
    entry do
      command '⌅'
      name 'new line'
    end
    entry do
      command '⌥ ⌅'
      name "new line (don't leave form box)"
    end

  end

  category do
    id 'Tab'

    entry do
      command '⇥'
      name 'tab'
    end
    entry do
      command '⌥ ⇥'
      name 'tab (dont leave form box)'
    end
    entry do
      command ' ⇤'
      name 'backtab'
    end
  end

  category do
    id 'Keyboard Settings'

    entry do
      command '⌘ space'
      name 'change languages'
    end
    entry do
      command '⌘⌃ space'
      name 'change input system'
    end
    entry do
      command '⌘⌥ space'
      name 'change keyboard layout'
    end
  end

  category do
    id 'Delete'

    entry do
      command '⌫'
      name 'delete backward'
    end
    entry do
      command '⌃ ⌫'
      name 'delete backward (an accent)'
    end
    entry do
      command '⌃⌥ ⌫'
      name 'delete backward one word'
    end
    entry do
      command '⌥ ⌫'
      name 'delete backward one word'
    end
    entry do
      command ' ⌦'
      name 'delete forward'
    end
    entry do
      command '⌥ ⌦'
      name 'delete forward one word'
    end
  end

  category do
    id 'Esc'

    entry do
      command ' esc'
      name 'cancel'
    end
    entry do
      command '⌥ esc'
      name 'complete current word'
    end
  end

  category do
    id 'Move / Select'

    entry do
      command ' ⇡'
      name 'move up'
    end
    entry do
      command '⇧ ⇡'
      name 'select upward'
    end
    entry do
      command '⌃ ⇡'
      name 'scroll one page up'
    end
    entry do
      command '⌘ ⇡'
      name 'move to the beginning of the document'
    end
    entry do
      command '⌘⇧ ⇡'
      name 'select to the beginning of the document'
    end
    entry do
      command '⌥ ⇡'
      name 'move to the beginning of the paragraph'
    end
    entry do
      command '⌥⇧ ⇡'
      name 'select to the beginning of the paragraph'
    end
    entry do
      command ' ⇣'
      name 'move down'
    end
    entry do
      command '⇧ ⇣'
      name 'select downward'
    end
    entry do
      command '⌃ ⇣'
      name 'scroll one page down'
    end
    entry do
      command '⌘ ⇣'
      name 'move to the end of the document'
    end
    entry do
      command '⌘⇧ ⇣'
      name 'select to the end of the document'
    end
    entry do
      command '⌥ ⇣'
      name 'move to the end of the paragraph'
    end
    entry do
      command '⌥⇧ ⇣'
      name 'select to the end of the paragraph'
    end
    entry do
      command ' ⇠'
      name 'move left'
    end
    entry do
      command '⇧ ⇠'
      name 'select leftward'
    end
    entry do
      command '⌃ ⇠'
      name 'move to the beginning of the line'
    end
    entry do
      command '⌃⇧ ⇠'
      name 'select to the beginning of the line'
    end
    entry do
      command '⌘ ⇠'
      name 'move to the beginning of the line'
    end
    entry do
      command '⌘⇧ ⇠'
      name 'select to the beginning of the line'
    end
    entry do
      command '⌘⌃ ⇠'
      name 'switch writing direction to right-to-left'
    end
    entry do
      command '⌥ ⇠'
      name 'move one word left'
    end
    entry do
      command '⌥⇧ ⇠'
      name 'select one word leftward'
    end
    entry do
      command ' ⇢'
      name 'move right'
    end
    entry do
      command '⇧ ⇢'
      name 'select rightward'
    end
    entry do
      command '⌃ ⇢'
      name 'move to the end of the line'
    end
    entry do
      command '⌃⇧ ⇢'
      name 'select to the end of the line'
    end
    entry do
      command '⌘ ⇢'
      name 'move to the end of the line'
    end
    entry do
      command '⌘⇧ ⇢'
      name 'select to the end of the line'
    end
    entry do
      command '⌘⌃ ⇢'
      name 'switch writing direction to left-to-right text'
    end
    entry do
      command '⌥ ⇢'
      name 'move one word right'
    end
    entry do
      command '⌥⇧ ⇢'
      name 'select one word rightward'
    end
  end

  category do
    id 'Scroll'

    entry do
      command ' ↖'
      name 'scroll to the beginning of the document'
    end
    entry do
      command '⇧ ↖'
      name 'select to the beginning of the document'
    end
    entry do
      command ' ↘'
      name 'scroll to the end of the document'
    end
    entry do
      command '⇧ ↘'
      name 'select to the end of the document'
    end
  end

  category do
    id 'Scroll (Pages)'

    entry do
      command ' ⇞'
      name 'scroll one page up'
    end
    entry do
      command '⇧ ⇞'
      name 'select one page up'
    end
    entry do
      command '⌥ ⇞'
      name 'move one page up'
    end
    entry do
      command ' ⇟'
      name 'scroll one page down'
    end
    entry do
      command '⇧ ⇟'
      name 'select one page down'
    end
    entry do
      command '⌥ ⇟'
      name 'move one page down'
    end
  end

  category do
    id 'Movement'

    entry do
      command '⌘ .'
      name 'cancel'
    end
    entry do
      command '⌃ A'
      name 'move to the beginning of the paragraph'
    end
    entry do
      command '⌃ B'
      name 'move backward'
    end
    entry do
      command '⌃ D'
      name 'delete forward'
    end
    entry do
      command '⌃ E'
      name 'move to the end of the paragraph'
    end
    entry do
      command '⌃ F'
      name 'move forward'
    end
    entry do
      command '⌃ H'
      name 'delete backward'
    end
    entry do
      command '⌃ K'
      name 'delete to the end of the paragraph'
    end
    entry do
      command '⌃ L'
      name 'center the selection in the text area'
    end
    entry do
      command '⌃ N'
      name 'move down'
    end
    entry do
      command '⌃ O'
      name 'split the current line'
    end
    entry do
      command '⌃ P'
      name 'move up'
    end
    entry do
      command '⌃ T'
      name 'transpose letters'
    end
    entry do
      command '⌃ V'
      name 'move one page down'
    end
    entry do
      command '⌃ Y'
      name 'yank back ‘killed’ text'
    end
  end
  notes <<-'END'
    * Original: [http://www.hcs.harvard.edu/~jrus/site/system-bindings.html](http://www.hcs.harvard.edu/~jrus/site/system-bindings.html)
    * Converted by [Kevin Traver](https://github.com/kevintraver)
  END

end
